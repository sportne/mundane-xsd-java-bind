# TASK-0094: 1.0.1-release

Status: active.

Task ID: `TASK-0094`
Priority: P0
Gate: `1.0.1` patch release.
Requirement IDs: `REQ-REL-002`, `REQ-QA-002`, `REQ-AGENT-001`
ADR IDs: `ADR-0011`, `ADR-0012`, `ADR-0013`
Specification references: `docs/infrastructure/release-plan.md`,
`docs/infrastructure/ci-plan.md`, `docs/build/release-consumption.md`,
`docs/infrastructure/release-notes-1.0.1.md`, `docs/requirements/traceability-matrix.md`
Target areas: release metadata, release workflow, release notes, release-consumer docs, README,
handoff, traceability, and release gates.
Allowed files: release metadata/docs/workflow, version metadata, release-consumer smoke version
plumbing, task/handoff/traceability docs.
Forbidden files: product behavior expansion, public API changes, dependency changes, Maven Central
publication, signing, remote staging repositories, support-claim expansion, and quality-gate
weakening.
Expected behavior: cut a `1.0.1` GitHub Release from `main` using only GitHub Release assets:
zipped Maven-layout staging repository, artifact manifest, release notes, and checksums.
Tests to add/update: release publication dry-run validation, release consumer smoke, full quality
gate, docs/design-control validation, and Native Image evidence where available.
Documentation to update: release notes, release plan, CI plan, release-consumption docs, README,
traceability, and handoff.
Commands to run: `./gradlew clean validateDesignControlPack qualityGate --console=plain`,
`./gradlew -Pmxjb.version=1.0.1 publicationDryRun --console=plain`,
`./gradlew releaseConsumerSmoke --console=plain`,
`bash -lc 'source "$HOME/.sdkman/bin/sdkman-init.sh" && ./gradlew validateDesignControlPack nativeSmoke nativeConformance --console=plain'`,
and `git diff --check`.
Acceptance criteria: `1.0.1` version metadata and release notes are present; release workflow
supports `v1.0.1` tags without re-hard-coding a single version; local release gates pass; subagent
review findings are addressed; `v1.0.1` is pushed after the release commit; GitHub Release workflow
is reviewed.
Rollback notes: if the release workflow fails, do not publish alternate artifacts manually; delete
any failed draft release/tag only after review, stage fixes locally, and do not create a replacement
release without rerunning the release gates.

Completion notes:
- Updated project version metadata to `1.0.1`.
- Made release workflow and release-consumer smoke use the candidate version instead of hard-coded
  `1.0.0` coordinates and asset names.
- Made the release workflow reject non-strict semantic-version release tags and tags that do not
  point at `origin/main`.
- Added `1.0.1` release notes preserving GitHub Release asset-only distribution and non-claims.
- Updated public release-consumption and release governance docs for the patch release.

Evidence:
- `./gradlew -Pmxjb.version=1.0.1 publicationDryRun --console=plain`
- `./gradlew releaseConsumerSmoke --console=plain`
- Initial `./gradlew clean validateDesignControlPack qualityGate --console=plain` exposed a stale
  design-control snippet expecting the `1.0.0` release-consumption example and a Gradle daemon lock
  under generated `build/release-consumer-smoke`; the snippet was updated and daemons were stopped.
- `./gradlew clean validateDesignControlPack qualityGate --console=plain`
- `bash -lc 'source "$HOME/.sdkman/bin/sdkman-init.sh" && ./gradlew validateDesignControlPack nativeSmoke nativeConformance --console=plain'`
- Native Image output still reports known experimental resource-option warnings; no native test
  failure occurred.
- Subagent release-diff review found premature accepted/completed task wording, GitHub Release zip
  layout mismatch with `releaseConsumerSmoke`, and missing strict tag/main-head validation; findings
  were addressed before commit.
- A later clean gate rerun exposed that `releaseConsumerSmoke` could leave a nested Gradle daemon
  lock under generated build output; the consumer smoke now runs nested Gradle with `--no-daemon`.
- Rerun `./gradlew -Pmxjb.version=1.0.1 publicationDryRun --console=plain`
- Rerun `./gradlew releaseConsumerSmoke --console=plain`
- Rerun `./gradlew clean validateDesignControlPack qualityGate --console=plain`
- Final affected validation after workflow fetch hardening:
  `./gradlew -Pmxjb.version=1.0.1 publicationDryRun --console=plain`,
  `./gradlew validateDesignControlPack qualityGate --console=plain`, and `git diff --check`.
- Pending: commit, push, `v1.0.1` tag push, and GitHub Release workflow review.
