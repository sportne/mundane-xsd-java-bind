# TASK-0058: xsd10-1.0-blocker-sequence

Status: accepted.

Task ID: `TASK-0058`
Gate: 1.0.0 blocker-sequence planning.
Baseline: accepted `TASK-0057` delta hardening and negative `TASK-0056` full-XSD readiness
decision.
Target areas: full-XSD blocker matrix, 1.0.0 release criteria, follow-on task cards, handoff,
release and CI planning, verification planning, compatibility profiles, and traceability.
Allowed files: planning docs, task cards, handoff, release/CI/verification/conformance docs, and
traceability.
Forbidden files: runtime/compiler behavior, generated output, release workflow, version bump,
release tag, publication behavior, signing, Maven Central, dependency changes, or `qualityGate`
weakening.
Expected behavior: define a decision-complete sequence for reaching `1.0.0` only after
`XP-XSD10-FULL` becomes executable and W3C generated-binding evidence is mapped and passing.
Tests to add/update: documentation/design-control checks only.
Commands to run: `./gradlew validateDesignControlPack qualityGate --console=plain`,
`git diff --check`.
Acceptance criteria: task cards `TASK-0059` through `TASK-0066` exist with clear allowed/forbidden
scope, tests, acceptance criteria, and rollback notes; docs agree that no `1.0.0` release,
workflow, tag, or full-XSD claim exists yet; handoff identifies `TASK-0059` as the next gate.
Rollback notes: revert this task card, follow-on task cards, and documentation updates.

## Completion notes

Accepted on 2026-05-24.

The `1.0.0` release bar is executable `XP-XSD10-FULL` generated-binding support for the accepted
product scope plus W3C generated-binding evidence, not a stable-subset release. The sequence remains
gated: release workflow, version update, tag creation, and GitHub Release artifact upload are
deferred to `TASK-0066` after full-XSD blockers and W3C generated-binding evidence pass.

Follow-on gates:

- `TASK-0059`: grouped content-list model support.
- `TASK-0060`: complete content-model automata and UPA validation.
- `TASK-0061`: full derivation, restriction, block/final, and `xsi:type` dispatch.
- `TASK-0062`: strict/lax wildcard deep validation and wildcard derivation composition.
- `TASK-0063`: remaining datatype, nil, and identity-validation edges.
- `TASK-0064`: W3C generated-binding row mapping and execution.
- `TASK-0065`: enable executable `XP-XSD10-FULL` only after evidence passes.
- `TASK-0066`: final `1.0.0` readiness, version update, release notes, and GitHub Release workflow.

No production behavior, dependency metadata, quality gates, version, release workflow, tag, or
publication behavior changed in this task.
