# TASK-0066: 1.0-readiness-release-workflow

Status: accepted.

Task ID: `TASK-0066`
Gate: final 1.0.0 readiness, version metadata, and GitHub Release workflow.
Target areas: README and public docs, release notes, Gradle version metadata, publication dry-run,
GitHub Actions release workflow, traceability, handoff, and final evidence.
Allowed files: docs, Gradle release metadata, GitHub workflow files, release notes, task/handoff
state, and release validation scripts if needed.
Forbidden files: product behavior, schema support changes, Maven Central publishing, signing,
secrets, package registry publishing, XSD 1.1, XML 1.1, canonical XML, DOM-backed binding, or
quality-gate weakening.
Expected behavior: declare `1.0.0` only after `XP-XSD10-FULL` is executable and W3C mapped
generated-binding evidence passes; add a tag-triggered GitHub Release workflow that stages
publishable Maven-layout artifacts locally and uploads the staging zip, checksums, release notes,
and artifact manifest to GitHub Release assets.
Tests to add/update: release-workflow static checks, publication dry-run validation for
`-Pmxjb.version=1.0.0`, docs/design-control checks, and final full local gates.
Commands to run: `./gradlew clean validateDesignControlPack qualityGate --console=plain`,
`./gradlew benchmarkSmoke --console=plain`,
`./gradlew validateDesignControlPack nativeSmoke nativeConformance --console=plain`,
`./gradlew -Pmxjb.w3cXsd10SuiteDir=/path/to/xmlschema2006-11-06 w3cXsd10Conformance --console=plain`,
`./gradlew -Pmxjb.version=1.0.0 publicationDryRun --console=plain`, `git diff --check`.
Acceptance criteria: public docs and release notes accurately claim full practical XSD 1.0
generated-binding support and preserve non-goals; local release dry-run stages only approved
coordinates; GitHub workflow publishes only GitHub Release assets from `v1.0.0` tags; no Maven
Central/signing/secrets are introduced.
Rollback notes: revert version/release workflow/docs changes and delete any local release staging
artifacts. If a pushed release CI run fails, make a single local follow-up fix and stage it for
maintainer review before any second push.

## Completion Notes

Accepted final `1.0.0` readiness for the current generated-binding product scope. The task updates
version metadata to `1.0.0`, adds `docs/infrastructure/release-notes-1.0.0.md`, reconciles public
and contributor docs, and adds `.github/workflows/release.yml` for tag-triggered GitHub Release
assets only. The workflow runs the JVM quality gate and `publicationDryRun`, stages the Maven-layout
repository locally, produces checksums and an artifact manifest, and creates a GitHub Release from
`v1.0.0` tags. Maven Central publication, signing, remote staging repositories, XSD 1.1, XML 1.1,
XML Canonicalization, lexical prefix preservation, DTD/entity identity, and DOM-backed binding
remain explicit non-goals.

Native conformance evidence exposed two release-readiness issues and both were fixed in this task:
the native unsupported-diagnostic fixture list still referenced schemas that are now supported by
`XP_XSD10_FULL`, and the resolver-denial path needed the JDK Xerces XML message resource bundle in
the native image. The native lane now runs with the current `redefine` unsupported fixture and
includes `com.sun.org.apache.xerces.internal.impl.msg.XMLMessages`.

Verification evidence:

- `./gradlew benchmarkSmoke --console=plain` passed. Representative advisory output included
  `xp-data-10-purchase-read-write-validate` at `38.860 opsPerSecond`,
  `xp-data-10-multins-read-write-validate` at `20.900 opsPerSecond`,
  `xp-xsd10-document-wildcard-read-write-validate` at `22.163 opsPerSecond`, and
  `xp-xsd10-document-mixed-read-write-validate` at `38.885 opsPerSecond`.
- `bash -lc 'source "$HOME/.sdkman/bin/sdkman-init.sh" && command -v native-image && ./gradlew validateDesignControlPack nativeSmoke nativeConformance --console=plain'` passed with
  `native-image` at `/home/jack/.sdkman/candidates/java/current/bin/native-image` and
  `NATIVE_CONFORMANCE status=passed supportedFixtures=9 unsupportedFixtures=1`.
- `./gradlew -Pmxjb.w3cXsd10SuiteDir=/mnt/d/projects/mundane-xsd-java-bind/build/tmp/w3c-xsd/extract/xmlschema2006-11-06 w3cXsd10Conformance --console=plain` passed with
  `total=24796 binding-supported=3 validation-only=24436 tolerated-metadata=98 expected-diagnostic=2 product-scope-incompatible=167 blocked=90` and
  `w3c-xsd10-binding-execution passed=1`.
- `./gradlew -Pmxjb.version=1.0.0 publicationDryRun --console=plain` passed and validated
  `9` staged publication coordinates for `1.0.0`.
- `./gradlew clean validateDesignControlPack qualityGate --console=plain` passed.
- `git diff --check` passed.

No further implementation gate is open in the current sequence. Future product, release, or
hardening work should begin with a new task card.
