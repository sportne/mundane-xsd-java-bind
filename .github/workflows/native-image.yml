name: native-image

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  native:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: [ '21', '25' ]
    steps:
      - uses: actions/checkout@v6
      - uses: graalvm/setup-graalvm@v1.4.5
        with:
          java-version: ${{ matrix.java }}
          distribution: 'graalvm'
          native-image-job-reports: 'true'
      - uses: gradle/actions/setup-gradle@v5.0.1
      - name: Native wiring check
        run: ./gradlew validateDesignControlPack
      # Product native tests are enabled after generated binding samples exist.
