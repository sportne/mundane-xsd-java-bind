/**
 * Dependency-free runtime primitives used by generated bindings.
 *
 * <p>This package intentionally avoids XML parser APIs, reflection-based binding, service
 * discovery, and third-party dependencies. Adapter modules can bridge parser implementations to
 * these interfaces without making generated code depend on those adapters.
 */
package io.github.mundanej.mxjb.runtime;
