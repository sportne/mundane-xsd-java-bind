# generator-cli

Command-line generator entry point.

## Current status

Scaffold only. The application plugin is wired, but no main class exists yet.

## Contributor notes

- Set the application main class only when the CLI implementation task is approved.
- CLI behavior must be backed by generator API/core tests.
- Keep command examples in README and docs synchronized.
